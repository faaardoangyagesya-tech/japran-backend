{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "sh -c 'php artisan serve --host=0.0.0.0 --port=${PORT:-8080}'",
        "healthcheckPath": "/",
        "healthcheckTimeout": 100
    }
}
