{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "sh -c 'php -S 0.0.0.0:${PORT:-8080} -t public'",
        "healthcheckPath": "/api/games",
        "healthcheckTimeout": 100
    }
}
