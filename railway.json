{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "sh -c 'php -S 0.0.0.0:${PORT:-8080} -t public public/router.php'",
        "healthcheckPath": "/",
        "healthcheckTimeout": 100
    }
}
