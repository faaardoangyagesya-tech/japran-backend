{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT:-8080} 2>&1",
        "healthcheckPath": "/",
        "healthcheckTimeout": 100
    }
}
